import 'package:flutter/material.dart';

/// Colores principales de la aplicación.
/// Aquí centralizamos la paleta para mantener consistencia
/// y facilitar cambios de diseño en el futuro.
class AppColors {

  AppColors._(); // evita crear instancias de esta clase

  // Colores de marca
  static const primary = Color(0xFF00C853);
  static const primaryDark = Color(0xFF009624);

  // Colores neutros
  static const black = Color(0xFF121212);
  static const darkGrey = Color(0xFF2C2C2C);
  static const background = Color(0xFFF5F7FA);

  // Colores usados en finanzas
  static const income = Color(0xFF00E676);
  static const expense = Color(0xFFFF5252);

}